Contains logic for the login, signup, and lougout authentication. The tools being user are:

- Next.js for rerouting
- Prisma for database accessing using TypeScript(in the lib folder)
- Zod for filtering validation (ex. "incorrect character count.")
- l
